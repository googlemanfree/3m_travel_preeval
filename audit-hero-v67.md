# Audit CTA héros v67

Le CTA Procédure utilise `/procedures`, qui rend l’annuaire complet des destinations. Un alias `/procedure` a été ajouté pour les anciens liens au singulier et redirige vers `/procedures`.

Le CTA visible e‑Visa utilise `/evisas`, qui rend l’annuaire e‑Visa complet avec filtres, destinations et fiches détaillées. Un alias `/e-design` a été ajouté pour les anciens liens utilisant ce libellé et redirige vers `/evisas`.

Tests de navigation effectués : `/procedure` → `/procedures` avec 107 destinations ; `/e-design` → `/evisas` avec le catalogue e‑Visa et ses fiches. Aucun écran vide constaté.
